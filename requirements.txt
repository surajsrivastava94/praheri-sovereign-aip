# Praheri starter dependencies
pydantic>=2.6
networkx>=3.2
pyvis>=0.3.2
streamlit>=1.33
chromadb>=0.5
Faker>=24.0
pandas>=2.2
requests>=2.31
python-dotenv>=1.0
# Llama is served separately via Ollama (https://ollama.com) — not a pip package.
# Optional production serving: vllm, or NVIDIA NIM. The MODEL stays Llama.
