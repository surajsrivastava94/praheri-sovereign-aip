# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env / secrets
.env
.env.*

# Data & local stores (synthetic, regenerate with `python -m praheri.generate`)
*.db
*.sqlite
chroma/
.chroma/
data/

# Streamlit / pyvis artifacts
.streamlit/secrets.toml
*.html
# …except the standalone explainer/landing page (a committed deliverable).
!app/explainer.html

# OS / editor
.DS_Store
.idea/
.vscode/

# generated / runtime artifacts
praheri.db
audit_log.jsonl
# demo_cache is runtime-regenerated, EXCEPT the committed golden-cache narratives
# for the shallow verticals (U10) — demo replay artifacts checked in so a fresh
# machine gets crash-proof vertical replay without a live model call.
demo_cache/*
!demo_cache/insurance_siu__GAR-RING-01.json
!demo_cache/lending_ews__DIR-RING-01.json
!demo_cache/wealth__ADV-RING-01.json
!demo_cache/corporate__CO-A.json

# pyvis-generated graph assets
lib/

# reference-only material (Build Bible source, original starter zip) — not the codebase
Cowork-Brainstorm/

# Netlify deploy staging (built from app/explainer.html)
.deploy/
